import React, { useState } from "react";
import data from "../data";
import shopTitle from "../data";
import styles from "../styles/ShopBtn.module.css";

//let showItem = () => {
   // let shopCards = data.map((card) => card.cards);
   //    console.log(shopCards);
   //    console.log(shopCards[0]);
   // let shopTitle = shopCards[0].map((item) => item.title);
   //    console.log(shopTitle);
   
//};

// let showItem = () => {
//    let result = data.filter((item)=> item.title === {title});
//    console.log(result);
//        }


// const ShopBtn = (props) => {
//   let [title, setTitle] = useState('a');
//   console.log({title})
//       return (
//    <div>
//    {data.map((shop) => (
//    <button
//       key={shop.id}
//       className={styles.item}
//       onClick={() => setTitle(shop.title)}
//    >
//       {shop.title}
//    </button>
//    ))}
// </div>
// );

// };


// export default ShopBtn;

